<div class="waiting--loader">
  <span class="waiting--loader--bull waiting--loader--bull1"></span>
  <span class="waiting--loader--bull waiting--loader--bull2"></span>
  <span class="waiting--loader--bull waiting--loader--bull3"></span>
</div>

<h1 class="waiting--title">{{title}}</h1>
<p class="waiting--text">{{text}}</p>
